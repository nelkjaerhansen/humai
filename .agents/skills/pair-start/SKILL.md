---
name: pair-start
description: Rehydrate Pair from the active plan and learning map
disable-model-invocation: true
---

## Working directory

Invoke from the **repository root**.

## Edit boundaries

Same as `/pair`: edit nothing.

Run a `/pair` session with this focus:

Rehydrate for pair programming.

1. Resolve the active plan under `humai-plans/implementing/` (exactly one; `refined-plan.md` or `work-notes.md`).
2. Summarize: plan title/goal, current slice if known, top open acceptance criterion or todo.
3. Note learning map / Learning blurb → teaching intensity posture and one material link if present, preferring a completed lesson when refresh would help.
4. State only materially missing context, if any.
5. Ask: ready?

Keep the whole reply short (default Pair brevity).
